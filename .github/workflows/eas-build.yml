# =============================================================================
# EAS BUILD WORKFLOW - DISABLED
# =============================================================================
#
# This workflow is disabled to preserve Expo free tier quota.
# EAS builds should be run locally instead:
#
#   eas build --platform ios --profile production
#   eas build --platform android --profile production
#
# If you have an Expo paid account and want to enable CI builds:
# 1. Add EXPO_TOKEN secret to GitHub repository settings
# 2. Uncomment the triggers below
# =============================================================================

name: EAS Build (Disabled)

# DISABLED - uncomment to enable
# on:
#   push:
#     tags:
#       - 'v*'
#   workflow_dispatch:
#     inputs:
#       profile:
#         description: 'Build profile'
#         required: true
#         default: 'preview'
#         type: choice
#         options:
#           - development
#           - preview
#           - production
#       platform:
#         description: 'Platform'
#         required: true
#         default: 'all'
#         type: choice
#         options:
#           - all
#           - ios
#           - android

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'This workflow requires EXPO_TOKEN. Run locally instead with: eas build'
        required: true
        type: boolean
        default: false

jobs:
  disabled-notice:
    name: EAS Build Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "⚠️ EAS builds are disabled in CI to preserve free tier quota."
          echo ""
          echo "Run builds locally instead:"
          echo "  eas build --platform ios --profile production"
          echo "  eas build --platform android --profile production"
          echo ""
          echo "See: https://docs.expo.dev/build/setup/"
          exit 1
